# root実行Pod（意図的に拒否される例）
apiVersion: v1
kind: Pod
metadata:
  name: root-pod
  namespace: k8s-secure
spec:
  containers:
  - name: root
    image: busybox
    command: ["sh", "-c", "sleep 3600"]
    securityContext:
      runAsUser: 0   # rootユーザーで実行
